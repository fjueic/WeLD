[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "WeLD"
version = "0.1.0"
description = "Web Layered Display"
authors = [{ name = "Rohit", email = "rohitmalik987789@gmail.com" }]
dependencies = [
    "PyGObject",
    "pydantic",
    "loguru",
]
requires-python = ">=3.8"

[tool.setuptools]
packages = ["weld"]

[project.scripts]
weldctl = "weld.cli:main"
weld = "weld:main"

[tool.setuptools.packages.find]
include = ["weld*"]
